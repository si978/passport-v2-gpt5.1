# 阿里云号码认证服务配置（Dypnsapi / SendSmsVerifyCode）
# 注意：生产环境请使用 Secret 管理（CI/CD、K8S Secret、云厂商密钥管理），不要把真实密钥提交到仓库。
ALIYUN_ACCESS_KEY_ID=
ALIYUN_ACCESS_KEY_SECRET=
ALIYUN_SMS_SIGN_NAME=
ALIYUN_SMS_TEMPLATE_CODE=

# 自建 HTTP 短信网关（可选，优先级低于阿里云配置；当 ALIYUN_* 不完整且 SMS_GATEWAY_URL 有值时启用）
SMS_GATEWAY_URL=
SMS_GATEWAY_KEY=

# 仅用于 Python PoC（Node 后端不会读取该开关）
SMS_TEST_MODE=true

